// Modern blue style theme
const theme = {
  // Primary colors
  primary: {
    main: '#3b82f6',     // Main blue
    light: '#60a5fa',    // Light blue
    dark: '#0a2540',     // Dark blue
    contrastText: '#ffffff', // Contrast text color (white)
  },
  
  // Background colors
  background: {
    default: '#f8fafc',  // Page background
    paper: '#ffffff',    // Component background
    sidebar: '#f1f5f9',  // Sidebar background
    hover: '#f1f5f9',    // Hover background
    disabled: '#e2e8f0', // Disabled state background
  },
  
  // Text colors
  text: {
    primary: '#0f172a',   // Primary text
    secondary: '#334155', // Secondary text
    muted: '#64748b',     // Muted text
    light: '#e0e0e0',     // Light text (for dark backgrounds)
    disabled: '#94a3b8',  // Disabled state text
  },
  
  // Border colors
  border: {
    light: '#e2e8f0',     // Light border
    default: '#cbd5e1',   // Default border
  },
  
  // Status colors
  status: {
    success: '#10b981',   // Success
    warning: '#f59e0b',   // Warning
    error: '#ef4444',     // Error
    info: '#0ea5e9',      // Information
  },
  
  // Shadows
  shadow: {
    sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
  }
};

export default theme; 